# Software environment

This directory documents the software and package versions used for the
Flag-Antp ChIP-seq analysis.

The analysis combines command-line bioinformatics programs, R packages,
and Python packages. The versions used in the original analysis are listed
in `software_versions.xlsx`.

Installation requirements specific to each analysis step are also described
in the corresponding README files under `scripts/`.
